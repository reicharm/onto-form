Bereite die aktuellen Änderungen für einen Commit vor:

1. Führe `npm test` aus – bei Fehlern stoppe und berichte
2. Zeige `git diff --stat` zur Übersicht
3. Erstelle einen präzisen Commit-Message-Vorschlag (was + warum, kein „was der Code tut")
4. Warte auf Bestätigung, bevor du commitest
